# .env.example - copy to .env locally and fill with real secrets (DO NOT commit .env)

DISCORD_TOKEN=
OPENAI_API_KEY=
TARGET_CHANNEL_ID=
ADMIN_IDS=             # comma separated discord IDs (optional)
BGS_PDF_URL=           # e.g. https://yourhost.com/bgs_reference.pdf
CACHE_TTL_HOURS=24

# Optional Upstash Redis (recommended for persistent KV)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
